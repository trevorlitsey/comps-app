TODO

Admin:
- hook up new event form to actually edit data, and add validators
- let user know that login is only for admins
- have user click to bring up field for editing venue info

- inlcude link for user to request if on page by accident

--info
- prevent user from setting non-unique access code

--events
- let user sort by date/event etc.
- let user edit existing events
- warn user if date submitted is similar to an old one

--toggle
- let user show/hide past events

Landing:
- implement server query for finding venue by access code

Login:
- what happens if user tries to sign in and they don't have an account?
- eventually do email / password login, too..

Register:
- generate venue passcode